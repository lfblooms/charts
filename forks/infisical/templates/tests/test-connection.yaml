apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infisical.fullname" . }}-test-connection"
  labels:
    {{- include "infisical.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '--timeout=5', '{{ include "infisical.fullname" . }}:{{ .Values.service.port }}/api/status']
  restartPolicy: Never
