services:
  registry:
    image: registry:3
    container_name: charts-registry
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      OTEL_TRACES_EXPORTER: none
    volumes:
      - registry-data:/var/lib/registry

volumes:
  registry-data:
