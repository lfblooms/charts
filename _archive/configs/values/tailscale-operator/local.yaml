# Tailscale Operator Local Development Values
# For KIND cluster testing

# Reduced resources for local testing
operatorConfig:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      memory: 128Mi

  # Debug logging for development
  logging: "debug"
