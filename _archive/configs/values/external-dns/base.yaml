# external-dns Base Values
# Fork: lfblooms/kubernetes-sigs.external-dns
# Chart: charts/external-dns

# Provider configuration (override per context)
# provider: cloudflare

# Policy for DNS record management
policy: sync

# Log level
logLevel: info

# Resources
resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi
